# Deployment

Host, runtime, build output, asset paths, publishing flow, rollback process, and authoritative verification signal are not configured yet.

Ask before installing app-runtime dependencies. Provide migrations as reviewable artifacts unless the user explicitly authorizes execution.
