# Decisions

### 2026-07-19: Codex-native project memory

Keep `AGENTS.md` as a thin kernel and route topical knowledge into nested `.agents/reference/` files loaded on demand. Project-memory capture does not authorize Git or external side effects.

### 2026-07-20: The Build Seam replaces the Proof Lab

Retire the near-black/vermilion wireframe-object identity, canvas renderer, evidence-ledger repetition, and Recursive fallback. Use one impossible Build Seam to transform the whole viewport across fluid, planar, articulated, and fused material laws. The official tagline and truthful locked shipment remain product contracts, not aesthetic constraints.

### 2026-07-20: Honest future work is designed, not empty

Keep three distinct pending project structures—portal, fold, and stack—until real work is supplied. Each state states its missing evidence and implies no client, outcome, metric, award, or experience claim. Real projects must inherit the structure rather than require another redesign.

### 2026-07-20: Native material system and local Anybody type

Use semantic DOM, modern CSS, deterministic phase tokens, and the guarded state machine without framework, canvas, WebGL, or animation dependencies. Vendor the official 201,352-byte Anybody variable font under OFL; its width and weight axes are functional lifecycle parameters, not decorative font novelty.

### 2026-07-20: Rendered critique is part of the acceptance gate

Two consecutive fresh Chrome critique passes must find no material issue across desktop, tablet, mobile, lifecycle states, pending work, keyboard focus, reduced motion, forced colors, and 200% zoom. If in-app Browser control is unavailable, isolated reviewers may use the installed Chrome engine through headless/CDP or Playwright and must visually inspect saved screenshots. Source-only review does not satisfy this gate.

### 2026-07-20: Build Seam clears local visual acceptance

Two consecutive independent Playwright/Chrome critiques found no material issue at 1440×900, 1280×720, 768×1024, 390×844, or the 720×450 reflow stress case. The locked Shipped explanation, keyboard path, reduced-motion state, pending project worlds, exit, and zero-overflow contract all passed; remaining hero collisions were classified as non-material art-direction pressure. This is local acceptance only—production shipment remains locked until deployment evidence exists.

### 2026-07-20: Time-Lapse Manufacture replaces the Build Seam

The Build Seam test direction failed the swap test (acid-poster neo-brutalist cluster; the seam was a stripe, not a law). The site is now a time-lapse build: scrolling is time, and one seeded raw-WebGL2 artifact (642 vertices, shared topology, fixed seed 20260720) morphs through vapor, blueprint, machine, and sealed-crate states. The rendered layer is enhancement only; semantic DOM, anchor controls, and SVG silhouettes carry full meaning without JavaScript, WebGL, motion, or pointer input. Three named review passes (recruiter, craft, anti-slop resilience) with fresh Chrome evidence are the acceptance gate; two consecutive clean passes were achieved.

### 2026-07-20: Placeholder forms renamed to unbuilt artifacts

Future cases keep their honesty contract but take manufacture-world forms: scaffold (product), blueprint fold (research), parts tray (experiment). Distinct silhouettes, explicit pending chips, no invented claims.
