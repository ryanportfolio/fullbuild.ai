apiVersion: v1
kind: Service
metadata:
  name: threadline-api
  labels:
    app.kubernetes.io/name: threadline-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: threadline-api
  ports:
    - name: http
      port: 80
      targetPort: http
