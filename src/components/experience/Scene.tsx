'use client';

// Placeholder — the real R3F walk-through (procedural pipeline model, barycentric
// wireframe, THE POUR section-dissolve, selective bloom) is built in the next
// pass. Until then the island renders nothing and the static sheets stand alone.
export default function Scene() {
  return null;
}
